---
phase: 02-book-catalog
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/components/BookCoverPlaceholder.jsx
  - frontend/src/components/AvailabilityBadge.jsx
  - frontend/src/components/BookCard.jsx
  - frontend/src/components/SearchBar.jsx
  - frontend/src/components/CategoryFilterSidebar.jsx
  - frontend/src/components/EmptyState.jsx
  - frontend/src/components/Toast.jsx
  - frontend/src/components/SalinanTable.jsx
  - frontend/src/pages/KatalogPage.jsx
  - frontend/src/pages/BukuDetailPage.jsx
  - frontend/src/router.jsx
  - frontend/src/index.css
autonomous: false
requirements: [CAT-01, CAT-02]
user_setup: []
must_haves:
  truths:
    - "A logged-in mahasiswa visiting /katalog sees a responsive grid of book cards populated from GET /api/buku"
    - "Typing in the search bar (debounced ~300ms) re-queries GET /api/buku?kata_kunci=... and updates the grid"
    - "Checking category checkboxes re-queries GET /api/buku?kategori=... and shows only those categories"
    - "Each card shows a category-colored placeholder cover, a binary Tersedia/Tidak Tersedia badge, and a Pinjam Buku button"
    - "Clicking an enabled Pinjam Buku button shows a coming-soon toast and creates no peminjaman"
    - "Clicking a card title navigates to /katalog/{id} showing buku info + a Salinan Buku table"
  artifacts:
    - path: "frontend/src/pages/KatalogPage.jsx"
      provides: "mahasiswa catalog browse grid with debounced search + category filter"
      contains: "KatalogPage"
    - path: "frontend/src/pages/BukuDetailPage.jsx"
      provides: "book detail view with salinan table"
      contains: "useParams"
    - path: "frontend/src/components/BookCard.jsx"
      provides: "card composing cover + badge + title link + pinjam button"
      contains: "BookCard"
    - path: "frontend/src/router.jsx"
      provides: "katalog and katalog/:id routes"
      contains: "katalog/:id"
  key_links:
    - from: "frontend/src/pages/KatalogPage.jsx"
      to: "/api/buku"
      via: "api.get('/buku', { params })"
      pattern: "api\\.get\\(['\"]/buku"
    - from: "frontend/src/components/BookCard.jsx"
      to: "/katalog/{id}"
      via: "Link to detail"
      pattern: "to=\\{?[`'\"]/katalog/"
---

<objective>
Deliver the mahasiswa catalog-discovery slice end-to-end: replace the ComingSoonPage at /katalog with a real KatalogPage (debounced search, category filter sidebar, responsive book grid, loading/empty/error states, coming-soon Pinjam toast) and add the /katalog/:id BukuDetailPage with a Salinan Buku table. Consumes the read API shipped in Plan 02-01.

Purpose: This is the user-facing payoff of CAT-01/CAT-02 — after this plan a mahasiswa can actually search, filter, and inspect books.
Output: 8 new presentational/feature components, 2 pages, router wiring, and a book-shadow CSS utility.
</objective>

## Phase Goal

**As a** mahasiswa, **I want to** search and filter the book catalog and open a book's detail, **so that** I can find and locate a book I want to borrow.

## Artifacts this phase produces

New symbols/files created by this plan (exclude from drift verification):
- Components: `BookCoverPlaceholder`, `AvailabilityBadge`, `BookCard`, `SearchBar`, `CategoryFilterSidebar`, `EmptyState`, `Toast`, `SalinanTable`
- Pages: `KatalogPage`, `BukuDetailPage`
- `frontend/src/index.css`: `.book-shadow` utility class
- `frontend/src/router.jsx`: `katalog` route now maps to `KatalogPage`; new `katalog/:id` route

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-book-catalog/02-CONTEXT.md
@.planning/phases/02-book-catalog/02-PATTERNS.md
@.planning/phases/02-book-catalog/02-UI-SPEC.md
@.planning/phases/02-book-catalog/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Presentational catalog components</name>
  <files>frontend/src/components/BookCoverPlaceholder.jsx, frontend/src/components/AvailabilityBadge.jsx, frontend/src/components/SearchBar.jsx, frontend/src/components/EmptyState.jsx, frontend/src/components/Toast.jsx, frontend/src/components/SalinanTable.jsx, frontend/src/index.css</files>
  <read_first>
    - frontend/src/components/RoleBadge.jsx (conditional-class chip pattern — exact shape for AvailabilityBadge/BookCoverPlaceholder)
    - frontend/src/components/InputField.jsx (icon-prefixed input technique, className conventions)
    - frontend/src/pages/ComingSoonPage.jsx (centered icon+heading+body layout for EmptyState)
    - frontend/src/index.css (current global styles — add .book-shadow only if absent)
    - .planning/phases/02-book-catalog/02-UI-SPEC.md (Color category-color mapping table ~lines 97-106; AvailabilityBadge ~lines 89-92; SearchBar pill styling ~line 187; EmptyState ~lines 197-198; Toast ~line 123,257; SalinanTable ~lines 236-239, status badge collapse)
    - .planning/phases/02-book-catalog/02-PATTERNS.md (AvailabilityBadge/BookCoverPlaceholder/SearchBar/EmptyState/SalinanTable blocks)
  </read_first>
  <action>
    Build six hand-built components (plain React + Tailwind, no shadcn) plus a CSS utility. Use ONLY tokens already in tailwind.config.js.
    - BookCoverPlaceholder({ kategori, tersedia, size }): div with `aspect-[2/3] w-full flex items-center justify-center` + category background tint + centered material-symbols `menu_book` icon. Category→color map (UI-SPEC): Fiksi→ink-blue, Non-Fiksi→antique-gold, Referensi→secondary-fixed/surface-container-high + on-surface-variant icon, fallback→sage-green. Icon size text-5xl on cards, text-7xl on detail (drive via `size` prop). No <img>, no external URLs.
    - AvailabilityBadge({ tersedia }): chip mirroring RoleBadge shape — tersedia → sage-green text + border-paper-shadow; not → surface-container/on-surface-variant + border-outline-variant. Text "Tersedia"/"Tidak Tersedia".
    - SearchBar({ value, onChange, placeholder }): icon-prefixed (`search`) input with pill styling `rounded-full bg-surface-container-low border border-paper-shadow focus:border-antique-gold focus:ring-1 focus:ring-antique-gold w-full pl-10 pr-4 py-2`. Placeholder "Cari judul, penulis, isbn...". Debounce is handled by the parent (KatalogPage) — keep this a controlled input OR debounce internally via useState+useEffect+setTimeout(300ms) cleanup and call an onSearch callback; document which approach is used.
    - EmptyState({ icon, heading, body, action }): centered `flex flex-col items-center justify-center min-h-[40vh] text-center`, material icon text-6xl, heading text-headline-md, body body-md, optional action slot. Used for no-results, fetch error (with Coba Lagi button), and empty salinan.
    - Toast({ message, onDismiss }): `role="status" aria-live="polite"`, `bg-primary text-on-primary rounded-DEFAULT`, fixed bottom-center/right, auto-dismiss ~3s via setTimeout. Local-state driven.
    - SalinanTable({ salinan }): striped table (columns Lokasi Rak | Kondisi | Status) per UI-SPEC. Render title-cased Indonesian labels for kondisi (Bagus/Rusak Ringan/Rusak Berat) and status (Tersedia/Dipesan/Dipinjam); Status as inline badge collapsing tersedia→sage-green chip, dipesan/dipinjam→neutral-grey chip. Compact empty state when salinan is empty.
    - frontend/src/index.css: add `.book-shadow { box-shadow: -2px 0 5px rgba(0,0,0,0.03), 0 4px 15px rgba(0,0,0,0.05); }` if not already present.
    All copy strings come from the UI-SPEC Copywriting Contract — Bahasa Indonesia.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/components/BookCoverPlaceholder.jsx src/components/AvailabilityBadge.jsx src/components/SearchBar.jsx src/components/EmptyState.jsx src/components/Toast.jsx src/components/SalinanTable.jsx 2>&1 | tail -5; test -f src/index.css && grep -q "book-shadow" src/index.css && echo "book-shadow ok"</automated>
  </verify>
  <acceptance_criteria>
    - All six component files exist and default-export a React component
    - AvailabilityBadge renders text "Tersedia" and "Tidak Tersedia" (grep both strings present)
    - BookCoverPlaceholder references `menu_book` and `aspect-[2/3]`
    - SearchBar input uses `rounded-full` and `focus:border-antique-gold`
    - SalinanTable contains the strings "Lokasi Rak", "Kondisi", "Status"
    - frontend/src/index.css contains `book-shadow`
    - `npx eslint` on the six files reports no errors
  </acceptance_criteria>
  <done>The reusable presentational layer (cover, badge, search, empty/error, toast, salinan table) exists, lint-clean, with all UI-SPEC copy and tokens.</done>
</task>

<task type="auto">
  <name>Task 2: BookCard + CategoryFilterSidebar + KatalogPage browse grid + router</name>
  <files>frontend/src/components/BookCard.jsx, frontend/src/components/CategoryFilterSidebar.jsx, frontend/src/pages/KatalogPage.jsx, frontend/src/router.jsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.jsx (useState/useEffect, api call, err.response?.data?.detail extraction)
    - frontend/src/layouts/AppShell.jsx (role read: getToken + decodeToken(token).peran, lines 18-21)
    - frontend/src/lib/api.js (axios instance baseURL /api, auth + 401 interceptor)
    - frontend/src/router.jsx (current katalog route mapped to ComingSoonPage)
    - frontend/src/components/RoleBadge.jsx (chip pattern, reused via AvailabilityBadge from Task 1)
    - .planning/phases/02-book-catalog/02-UI-SPEC.md (Layout & Navigation Contract: Jelajah grid ~lines 177-198, BookCard structure ~lines 192-195, filter sidebar ~lines 186-189, loading/empty/error states; availability filter is CLIENT-SIDE only ~line 189)
    - .planning/phases/02-01-... (read API shapes: GET /api/buku → { items:[{id,judul,penulis,isbn,kategori,tahun_terbit,tersedia}], total }; GET /api/buku/kategori → string[])
  </read_first>
  <action>
    Build BookCard, CategoryFilterSidebar, and KatalogPage (Jelajah browse grid only — the pustakawan Kelola tab is Plan 02-04), then wire routes.

    BookCard({ buku, onPinjam }): <article> per UI-SPEC with `border-l-4` category accent (muted to border-l-secondary-fixed when !tersedia), composes BookCoverPlaceholder (size card) with AvailabilityBadge overlaid `absolute top-2 right-2`; body shows category tag (text-antique-gold uppercase), title wrapped in `<Link to={`/katalog/${buku.id}`}>` (import { Link } from 'react-router'), author, ISBN (font-mono). Footer button: Sage-Green "Pinjam Buku" (bookmark_add icon) when buku.tersedia else disabled grey "Tidak Tersedia" (lock icon, aria-disabled). Enabled button calls onPinjam (no navigation, no API).

    CategoryFilterSidebar({ categories, selected, onChange, search, onSearch }): renders the SearchBar at top (mobile-first), then a "Kategori" panel (book-shadow card) with a "Semua Kategori" checkbox (clears all, mutually exclusive with individual selections) + one checkbox per category (multi-select OR). Then a "Ketersediaan" radio group (Semua Status / Tersedia) — client-side filter only, do NOT send to backend. Use real <input type="checkbox">/<input type="radio"> with <label> wrappers (gap-3) and antique-gold checked state. Section headers uppercase label-md.

    KatalogPage: useState for bukuList, categories, loading, error, searchTerm, selectedKategori (array), availabilityFilter. On mount fetch GET /api/buku/kategori for the sidebar. useEffect debounces searchTerm 300ms, then calls api.get('/buku', { params: { kata_kunci: searchTerm || undefined, kategori: selectedKategori } }) — pass kategori as a repeated param (axios array). Apply the availability radio client-side to the returned items. Render: page title "Katalog Buku" (headline-md), result count "Menampilkan {N} buku", main flex (CategoryFilterSidebar + responsive grid grid-cols-1 sm:2 lg:3 xl:4 gap-6). Loading → 8 skeleton cards; zero results → EmptyState (search_off, "Buku Tidak Ditemukan"); fetch error → EmptyState (error_outline, "Gagal Memuat Katalog") + Coba Lagi retry. A Toast shows "Fitur peminjaman akan tersedia di fase berikutnya." when a card's Pinjam Buku fires (local visibility state). Read peran via decodeToken but DO NOT render the Kelola tab here (deferred to Plan 02-04) — keep the page mahasiswa-shaped; Plan 02-04 will extend it.

    router.jsx: import KatalogPage and BukuDetailPage; replace the `{ path: 'katalog', element: <ComingSoonPage title="Katalog Buku" /> }` line with `{ path: 'katalog', element: <KatalogPage /> }` and add `{ path: 'katalog/:id', element: <BukuDetailPage /> }` immediately after, inside the same AppShell children array. Remove the now-unused ComingSoonPage import only if no other route uses it (it still serves pinjaman/dashboard/anggota — keep the import).
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/components/BookCard.jsx src/components/CategoryFilterSidebar.jsx src/pages/KatalogPage.jsx src/router.jsx 2>&1 | tail -5; grep -q "katalog/:id" src/router.jsx && grep -q "KatalogPage" src/router.jsx && echo "routes ok"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/router.jsx contains `katalog/:id` and maps `katalog` to `<KatalogPage`
    - KatalogPage contains `api.get('/buku'` and `api.get('/buku/kategori'`
    - KatalogPage debounces search (contains setTimeout and a 300 value)
    - BookCard contains a Link `to={`/katalog/` and the strings "Pinjam Buku" and "Tidak Tersedia"
    - CategoryFilterSidebar contains "Semua Kategori" and `type="checkbox"` and `type="radio"`
    - The Pinjam toast string "Fitur peminjaman akan tersedia di fase berikutnya." appears in KatalogPage
    - `npx eslint` on the four files reports no errors
  </acceptance_criteria>
  <done>/katalog renders a live, debounced, filterable mahasiswa grid backed by the real read API, with Pinjam showing a coming-soon toast.</done>
</task>

<task type="auto">
  <name>Task 3: BukuDetailPage</name>
  <files>frontend/src/pages/BukuDetailPage.jsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.jsx (useState/useEffect/api + error extraction)
    - frontend/src/components/SalinanTable.jsx (from Task 1 — consumed here)
    - frontend/src/components/BookCoverPlaceholder.jsx, AvailabilityBadge.jsx, EmptyState.jsx (from Task 1)
    - frontend/src/layouts/AppShell.jsx (role read pattern)
    - .planning/phases/02-book-catalog/02-UI-SPEC.md (Book Detail View ~lines 228-239: back link, header block, Salinan Buku section, D-07 visible to both roles)
    - .planning/phases/02-01-... (GET /api/buku/{id} shape: buku fields + tersedia + salinan[])
  </read_first>
  <action>
    Create BukuDetailPage using useParams() for :id. useEffect fetches api.get(`/buku/${id}`); track loading/error/buku state. Render: back link "← Kembali ke Katalog" (Link to /katalog), header block (flex-col md:flex-row) with a large BookCoverPlaceholder (size detail, text-7xl, border-l-4 category accent) on the left and on the right the category tag, title (headline-md), author, a label-sm metadata list ("ISBN: {isbn}" font-mono, "Tahun Terbit: {tahun_terbit}", "Kategori: {kategori}"), and the AvailabilityBadge near the title. Then a "Salinan Buku" section header (uppercase label-md) and <SalinanTable salinan={buku.salinan} /> (which renders its own compact empty state for zero copies). Loading → skeleton/placeholder; fetch error → EmptyState error_outline + Coba Lagi. Read peran via decodeToken but do NOT render the pustakawan "Tambah Salinan" form or "Edit" button here — those are Plan 02-04 (pustakawan slice). Keep mahasiswa-shaped; Plan 02-04 extends this page. Use the err.response?.data?.detail || '<Indonesian fallback>' idiom for errors.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/pages/BukuDetailPage.jsx 2>&1 | tail -5; grep -q "useParams" src/pages/BukuDetailPage.jsx && grep -q "SalinanTable" src/pages/BukuDetailPage.jsx && echo "detail ok"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/BukuDetailPage.jsx contains `useParams`, ``api.get(`/buku/${`` (template fetch), and `<SalinanTable`
    - Contains the back-link string "Kembali ke Katalog"
    - Renders the metadata labels "Tahun Terbit" and "Kategori"
    - `npx eslint src/pages/BukuDetailPage.jsx` reports no errors
  </acceptance_criteria>
  <done>/katalog/:id shows buku info + the per-copy Salinan table for both roles, fed by GET /api/buku/{id}.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A real /katalog browse experience (debounced search, category multi-select, availability radio, responsive 1→4 col grid, coming-soon Pinjam toast) and a /katalog/:id detail view with a Salinan table — fed by the seeded read API.</what-built>
  <how-to-verify>
    1. Ensure backend + DB are running and seeded (`docker-compose up`, then run the seed if not auto-run), and the frontend dev server is up.
    2. Log in as the seeded mahasiswa (register one if needed) and open /katalog.
    3. Confirm a grid of seeded books renders; each card shows a colored placeholder cover, category tag, ISBN, and a Tersedia/Tidak Tersedia badge. At least one book shows "Tidak Tersedia".
    4. Type "sapiens" in the search bar — after a brief pause the grid narrows to the Sapiens book. Clear it; results return.
    5. Check a category checkbox (e.g. Fiksi) — only Fiksi books remain. Check a second category — both appear. Click "Semua Kategori" — all return.
    6. Switch the Ketersediaan radio to "Tersedia" — unavailable books disappear.
    7. Click an enabled "Pinjam Buku" — a toast "Fitur peminjaman akan tersedia di fase berikutnya." appears and auto-dismisses; no error.
    8. Click a book title — /katalog/:id opens showing the larger cover, metadata, and a Salinan Buku table (or the empty-copies message). Use the back link to return.
    9. Narrow the browser to 375px — sidebar stacks above a single-column grid; nothing overflows.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/buku | Authenticated fetches from the React app via the shared axios instance |
| URL path → BukuDetailPage | User-controlled `:id` route param fed into GET /api/buku/{id} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure (IDOR) | /katalog/:id → GET /api/buku/{id} | accept | Catalog detail is non-sensitive shared data visible to all authenticated roles (D-02/D-07); no per-user ownership exists for buku, so no IDOR surface. Backend still 404s unknown ids. |
| T-02-05 | Elevation of Privilege (UI) | KatalogPage/BukuDetailPage role gating | mitigate | This plan renders only mahasiswa-facing UI; no mutating actions are exposed. Pustakawan admin UI and its server-side role guard arrive in Plans 02-03/02-04 — frontend role checks are never the security boundary |
| T-02-06 | Tampering (XSS) | Rendering buku.judul/penulis/kategori | mitigate | Values rendered as React text children (auto-escaped); no dangerouslySetInnerHTML, no cover image URLs (D-13 placeholders only) |
| T-02-SC | Tampering | npm installs | accept | No new npm packages — reuses axios/react-router already vetted in Phase 1; no install task present |
</threat_model>

<verification>
- `npx eslint` clean on all created files
- /katalog renders seeded books; search and category filter re-query the backend; availability radio filters client-side
- Pinjam Buku shows the coming-soon toast and creates no peminjaman
- /katalog/:id renders buku info + salinan table; responsive at 375px
</verification>

<success_criteria>
- CAT-01: mahasiswa can search by judul/penulis/isbn and see matching results (visible end-to-end)
- CAT-02: mahasiswa can filter by kategori and see only those books
- Detail view shows per-copy table (D-07); Pinjam is a styled coming-soon stub (D-06)
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/02-book-catalog/02-02-SUMMARY.md` when done
</output>
