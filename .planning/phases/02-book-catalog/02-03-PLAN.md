---
phase: 02-book-catalog
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/routers/buku.py
  - backend/tests/test_buku.py
autonomous: true
requirements: [CAT-03, CAT-04]
must_haves:
  truths:
    - "A pustakawan can POST /api/buku to create a master book; a mahasiswa POSTing the same gets 403"
    - "A pustakawan can PUT /api/buku/{id} to edit a book; unknown id returns 404; duplicate ISBN returns 409"
    - "A pustakawan can DELETE /api/buku/{id} when it has no copies (204); deletion is blocked with 409 when salinan_buku copies exist"
    - "A pustakawan can POST /api/buku/{id}/salinan to add a physical copy with lokasi_rak, kondisi, status_ketersediaan"
    - "Duplicate ISBN on create returns 409 with an Indonesian detail"
  artifacts:
    - path: "backend/app/routers/buku.py"
      provides: "POST/PUT/DELETE /api/buku and POST /api/buku/{id}/salinan with pustakawan role-gating"
      contains: "def tambah_buku"
    - path: "backend/tests/test_buku.py"
      provides: "CRUD + role-gate + FK-safe-delete tests"
      contains: "def test_create_buku_forbidden_for_mahasiswa"
  key_links:
    - from: "backend/app/routers/buku.py mutation handlers"
      to: "PeranPengguna.pustakawan"
      via: "inline 403 role check"
      pattern: "peran != PeranPengguna\\.pustakawan"
    - from: "DELETE /api/buku/{id}"
      to: "salinan_buku count guard"
      via: "FK-safe block"
      pattern: "SalinanBuku.*count\\(\\)"
---

<objective>
Add the pustakawan catalog-management write side to the existing `/api/buku` router: create/edit/delete master `buku` (CAT-03) and add physical `salinan_buku` copies (CAT-04), all gated to `pustakawan`, with FK-safe delete and duplicate-ISBN handling. Extends the read router from Plan 02-01.

Purpose: This is the server-side capability that lets the librarian curate the catalog — the backend half of the pustakawan slice that Plan 02-04 surfaces in the UI.
Output: Mutation endpoints appended to `backend/app/routers/buku.py` and CRUD/role/FK tests appended to `backend/tests/test_buku.py` (GREEN).
</objective>

## Phase Goal

**As a** pustakawan, **I want to** add, edit, and delete master books and add physical copies, **so that** the catalog mahasiswa browse stays accurate and stocked.

## Artifacts this phase produces

New symbols created by this plan (exclude from drift verification):
- `backend/app/routers/buku.py`: `tambah_buku` (POST ""), `ubah_buku` (PUT "/{id_buku}"), `hapus_buku` (DELETE "/{id_buku}"), `tambah_salinan` (POST "/{id_buku}/salinan")
- `backend/tests/test_buku.py`: `test_create_buku_pustakawan`, `test_create_buku_forbidden_for_mahasiswa`, `test_create_buku_duplicate_isbn`, `test_edit_buku`, `test_delete_buku_no_salinan`, `test_delete_buku_with_salinan_blocked`, `test_add_salinan`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-book-catalog/02-CONTEXT.md
@.planning/phases/02-book-catalog/02-PATTERNS.md
@.planning/phases/02-book-catalog/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — CRUD, role-gate, and FK-safe-delete tests</name>
  <files>backend/tests/test_buku.py</files>
  <read_first>
    - backend/tests/test_buku.py (the read tests from Plan 02-01 — append, do not rewrite; reuse its auth-header helper style)
    - backend/tests/test_auth.py (register→login→Bearer pattern)
    - backend/tests/conftest.py (client + db_session fixtures)
    - backend/app/models/pengguna.py + enums.py (Pengguna, PeranPengguna.pustakawan for seeding a librarian directly via db_session)
    - .planning/phases/02-book-catalog/02-CONTEXT.md (D-04 add-only salinan, FK-safe-delete contract in Claude's Discretion)
    - .planning/phases/02-book-catalog/02-PATTERNS.md (CRUD/IntegrityError, FK-safe delete, 403 role-gate pattern blocks)
  </read_first>
  <behavior>
    - test_create_buku_pustakawan: with a Bearer token for a directly-seeded pustakawan, POST /api/buku {judul,penulis,isbn(valid 13-digit),kategori,tahun_terbit} returns 201 and the created book is retrievable via GET /api/buku.
    - test_create_buku_forbidden_for_mahasiswa: same POST with a mahasiswa token returns 403.
    - test_create_buku_duplicate_isbn: POSTing a second book with an existing ISBN returns 409.
    - test_edit_buku: PUT /api/buku/{id} as pustakawan updates judul; unknown id → 404.
    - test_delete_buku_no_salinan: DELETE /api/buku/{id} of a copy-less book as pustakawan returns 204 and the book no longer appears.
    - test_delete_buku_with_salinan_blocked: a book with >=1 salinan_buku → DELETE returns 409 and the book still exists.
    - test_add_salinan: POST /api/buku/{id}/salinan as pustakawan {lokasi_rak,kondisi,status_ketersediaan} returns 201 and the copy appears in GET /api/buku/{id}'s salinan list; same POST as mahasiswa returns 403.
  </behavior>
  <action>
    Append these tests to backend/tests/test_buku.py (keep the read tests from Plan 02-01 intact). Add a small helper to mint a pustakawan token: insert a Pengguna with peran=PeranPengguna.pustakawan and a hashed password via db_session (reuse hash_password from app.core.security), then POST /api/autentikasi/masuk to get the token. Reuse the existing mahasiswa-token approach for the 403 cases. Assert exact status codes per <behavior>. These tests MUST fail (RED) because the mutation endpoints do not exist yet — do not implement them in this task.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_buku.py -k "create or edit or delete or salinan" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_buku.py contains test_create_buku_pustakawan, test_create_buku_forbidden_for_mahasiswa, test_create_buku_duplicate_isbn, test_edit_buku, test_delete_buku_no_salinan, test_delete_buku_with_salinan_blocked, test_add_salinan
    - The read tests from Plan 02-01 remain present (grep test_search_buku still found)
    - `python -m pytest tests/test_buku.py -k "create or edit or delete or salinan"` exits non-zero (RED — endpoints missing)
  </acceptance_criteria>
  <done>The seven CRUD/role/FK tests exist and fail because the mutation endpoints are not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — pustakawan mutation endpoints</name>
  <files>backend/app/routers/buku.py</files>
  <read_first>
    - backend/app/routers/buku.py (read endpoints + router from Plan 02-01 — append handlers; keep /kategori before /{id_buku})
    - backend/app/routers/autentikasi.py (IntegrityError→409 shape, HTTPException conventions)
    - backend/app/schemas/buku.py (BukuCreate, BukuUpdate, SalinanBukuCreate, BukuOut, SalinanBukuOut from Plan 02-01)
    - backend/app/dependencies/auth.py (get_current_user → Pengguna with .peran)
    - backend/app/models/enums.py (PeranPengguna.pustakawan)
    - .planning/phases/02-book-catalog/02-PATTERNS.md (CRUD/IntegrityError, FK-safe delete, 403 role-gate, 404-lookup blocks ~lines 87-146)
  </read_first>
  <action>
    Append four handlers to the existing router (all depend on get_current_user). At the top of each handler body enforce role: `if user.peran != PeranPengguna.pustakawan: raise HTTPException(status.HTTP_403_FORBIDDEN, detail="Akses ditolak.")`.
    - POST "" (tambah_buku, status_code=201, response_model=BukuOut): body BukuCreate; create Buku; on commit IntegrityError (duplicate ISBN) → rollback + 409 detail "ISBN ini sudah terdaftar pada buku lain."; also do a pre-check query for an existing ISBN → 409 same detail.
    - PUT "/{id_buku}" (ubah_buku, response_model=BukuOut): id_buku: uuid.UUID; lookup-or-404 ("Buku tidak ditemukan."); update judul/penulis/isbn/kategori/tahun_terbit from BukuUpdate; commit with IntegrityError→409 (duplicate ISBN message above).
    - DELETE "/{id_buku}" (hapus_buku, status_code=204): lookup-or-404; count SalinanBuku where id_buku == id; if count > 0 → 409 detail f"Buku masih memiliki {count} salinan fisik."; else db.delete(buku) + commit (FK-safe block per CONTEXT.md, the safer option — no cascade).
    - POST "/{id_buku}/salinan" (tambah_salinan, status_code=201, response_model=SalinanBukuOut): lookup the buku-or-404; body SalinanBukuCreate; create SalinanBuku(id_buku=buku.id, ...); commit/refresh; return the new copy. Add-only per D-04 (no edit/delete of copies this phase).
    Place the POST "" handler such that it does not shadow GET "" (different methods, fine on the same path). Run the Task 1 tests; iterate to GREEN. Do not modify the read endpoints.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_buku.py tests/test_auth.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/routers/buku.py contains `def tambah_buku`, `def ubah_buku`, `def hapus_buku`, `def tambah_salinan`
    - Each mutation handler contains `peran != PeranPengguna.pustakawan` and raises 403 "Akses ditolak."
    - hapus_buku contains a SalinanBuku `.count()` guard raising 409 "...salinan fisik."
    - Duplicate-ISBN paths raise 409 "ISBN ini sudah terdaftar pada buku lain."
    - `cd backend && python -m pytest tests/test_buku.py tests/test_auth.py` exits 0 (all GREEN, read + auth unbroken)
  </acceptance_criteria>
  <done>All CRUD/role/FK tests pass; the catalog write API enforces pustakawan-only access, FK-safe delete, and duplicate-ISBN conflicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → mutating /api/buku endpoints | Authenticated writes (POST/PUT/DELETE) that must be restricted to pustakawan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Elevation of Privilege | POST/PUT/DELETE /api/buku, POST /{id}/salinan | mitigate | Each handler checks `user.peran != PeranPengguna.pustakawan` → 403 server-side; a mahasiswa token cannot mutate the catalog (covered by test_create_buku_forbidden_for_mahasiswa and the salinan 403 assertion) |
| T-02-08 | Tampering / Injection | BukuCreate/BukuUpdate/SalinanBukuCreate bodies | mitigate | Pydantic schemas validate types, lengths, ISBN 10/13-digit, tahun_terbit range; SQLAlchemy ORM binds all values (no raw SQL) |
| T-02-09 | Tampering (data integrity) | DELETE /api/buku/{id} | mitigate | FK-safe block: deletion refused with 409 while salinan_buku rows reference the book — prevents silent loss of physical-copy records (safer-option contract from CONTEXT.md) |
| T-02-10 | Spoofing/Replay | duplicate ISBN create | mitigate | Pre-check + IntegrityError→409 keeps ISBN unique (DB unique index from Phase 1 + app-level guard) |
| T-02-SC | Tampering | pip installs | accept | No new packages — reuses fastapi/sqlalchemy/pydantic from Phase 1; no install task present |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_buku.py tests/test_auth.py` exits 0
- A mahasiswa token gets 403 on POST /api/buku and POST /api/buku/{id}/salinan
- DELETE on a book with copies returns 409; on a copy-less book returns 204
- Duplicate ISBN create returns 409
</verification>

<success_criteria>
- CAT-03: pustakawan can create/edit/delete master buku (tests GREEN), FK-safe delete enforced
- CAT-04: pustakawan can add salinan_buku copies (tests GREEN)
- All write endpoints are pustakawan-gated server-side
</success_criteria>

<output>
Create `.planning/phases/02-book-catalog/02-03-SUMMARY.md` when done
</output>
