---
phase: 01-foundation-schema-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/requirements-dev.txt
  - backend/.env.example
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/0001_initial_schema.py
  - backend/app/__init__.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/app/models/__init__.py
  - backend/app/models/enums.py
  - backend/app/models/pengguna.py
  - backend/app/models/buku.py
  - backend/app/models/salinan_buku.py
  - backend/app/models/peminjaman.py
  - backend/app/seed.py
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_auth.py
autonomous: false
requirements: [AUTH-03, NFR-01]
user_setup: []
must_haves:
  truths:
    - "Running alembic upgrade head on a clean Postgres creates all 4 tables (pengguna, buku, salinan_buku, peminjaman) and all 4 ENUM types"
    - "The seed script creates exactly one pustakawan account and is safe to run twice (idempotent)"
    - "pytest collects backend/tests/test_auth.py and the 4 AUTH tests fail with a clear 'not implemented' signal (RED state) because no endpoints exist yet"
  artifacts:
    - path: "backend/alembic/versions/0001_initial_schema.py"
      provides: "Hand-written migration creating 4 ENUMs + 4 tables"
      contains: "peran_pengguna"
    - path: "backend/app/models/pengguna.py"
      provides: "Pengguna ORM model with bcrypt-hash column kata_sandi"
      contains: "class Pengguna"
    - path: "backend/app/seed.py"
      provides: "Idempotent pustakawan seed"
      contains: "ON CONFLICT|filter_by(email"
    - path: "backend/tests/test_auth.py"
      provides: "Failing AUTH-01..04 tests (Wave 0 RED scaffold)"
      contains: "def test_register"
    - path: "backend/tests/conftest.py"
      provides: "TestClient + test DB session fixtures"
      contains: "TestClient"
  key_links:
    - from: "backend/app/models/pengguna.py"
      to: "backend/alembic/versions/0001_initial_schema.py"
      via: "shared table/column/enum names (pengguna, kata_sandi, peran_pengguna)"
      pattern: "pengguna"
    - from: "backend/app/seed.py"
      to: "backend/app/models/pengguna.py"
      via: "import Pengguna model"
      pattern: "from app.models.pengguna import"
---

<objective>
Stand up the backend skeleton and the COMPLETE database foundation: SQLAlchemy 2.x models for all 4 tables + 4 ENUMs, a hand-written Alembic initial migration (per D-10, autogenerate is unreliable for PG enums), an idempotent pustakawan seed (D-11), pydantic-settings config (D-14), and the pytest Wave 0 scaffold with a FAILING test_auth.py covering AUTH-01..04.

This is the "real DB read/write" leg of the Walking Skeleton. After this plan, the schema can be applied to a clean Postgres and a pustakawan row exists; the auth endpoints that satisfy the failing tests are built in Plan 02.

Purpose: Establish the schema and ORM source-of-truth every later phase builds on (D-10), and lay the RED tests Plan 02 turns GREEN.
Output: Backend package structure, requirements files, migration, models, seed, pytest config, and failing auth tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema-auth/01-CONTEXT.md
@.planning/phases/01-foundation-schema-auth/01-RESEARCH.md
@.planning/phases/01-foundation-schema-auth/01-VALIDATION.md
@docs/PRD.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install backend dependencies and confirm against real registries</name>
  <read_first>
    - .planning/phases/01-foundation-schema-auth/01-RESEARCH.md (Standard Stack table + Installation block + Package Legitimacy Audit — ALL backend packages are tagged [ASSUMED] because pip was unavailable in the research env)
  </read_first>
  <files>backend/requirements.txt, backend/requirements-dev.txt</files>
  <what-built>
    Create backend/requirements.txt with the exact runtime packages from RESEARCH.md Standard Stack: fastapi, uvicorn[standard], sqlalchemy>=2.0, alembic, psycopg[binary], pydantic-settings, pyjwt, pwdlib[bcrypt], python-multipart, python-dotenv. Create backend/requirements-dev.txt with: pytest, httpx (httpx is required by FastAPI TestClient). Then run pip install against the real PyPI registry.
  </what-built>
  <action>
    Author backend/requirements.txt and backend/requirements-dev.txt using the package list from 01-RESEARCH.md (do NOT substitute passlib for pwdlib or python-jose for pyjwt — those substitutions are explicitly rejected in RESEARCH Pitfalls 2 and the Alternatives table). Re-verify versions at install time with `pip index versions <pkg>` since RESEARCH version pins are WebSearch-derived. pwdlib is pre-1.0 (0.3.x) and flagged [ASSUMED] for extra scrutiny — confirm it installs and that `from pwdlib.hashers.bcrypt import BcryptHasher` imports cleanly. Run `pip install -r requirements.txt -r requirements-dev.txt`. Documented fallback if pwdlib fails: passlib[bcrypt] with bcrypt pinned >=3.1.3,<4.0.0 (RESEARCH Pitfall 2). This is a blocking human-verify gate because all packages are [ASSUMED] (slopcheck never ran) — addresses threat T-01-SC.
  </action>
  <how-to-verify>
    1. Run `pip install -r backend/requirements.txt -r backend/requirements-dev.txt` and confirm it completes with no resolution errors.
    2. Run `python -c "from pwdlib.hashers.bcrypt import BcryptHasher; import jwt; import fastapi, sqlalchemy, alembic; print('ok')"` and confirm it prints `ok`.
    3. Spot-check pwdlib and pyjwt legitimacy at pypi.org/project/pwdlib and pypi.org/project/pyjwt (real maintainers, recent releases).
  </how-to-verify>
  <verify>
    <automated>cd backend && python -c "from pwdlib.hashers.bcrypt import BcryptHasher; import jwt, fastapi, sqlalchemy, alembic; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/requirements.txt lists pyjwt and pwdlib[bcrypt] (NOT python-jose, NOT passlib)
    - backend/requirements-dev.txt lists pytest and httpx
    - The import smoke command prints `ok` with no ModuleNotFoundError
  </acceptance_criteria>
  <resume-signal>Type "approved" once all packages install and import cleanly, or report which package failed.</resume-signal>
  <done>All runtime + dev packages install from real registries; pwdlib BcryptHasher and PyJWT import successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLAlchemy models, config, DB engine, hand-written migration, seed</name>
  <read_first>
    - .planning/phases/01-foundation-schema-auth/01-RESEARCH.md (Pattern 1 SQLAlchemy 2.x models with create_type=False; Pattern 2 hand-written migration for 4 ENUMs + 4 tables; Pattern 7 idempotent seed; config.py + .env.example snippets; Pitfall 1 enum autogenerate; Recommended Project Structure)
    - .planning/phases/01-foundation-schema-auth/01-CONTEXT.md (D-09 Alembic, D-10 complete schema, D-11 one pustakawan seed, D-12 password min-length is backend-enforced — note it lives in Plan 02's schema, D-14 .env secrets)
    - docs/PRD.md (§4 full data model: 4 tables, 4 ENUMs, FK relationships, column names in Bahasa Indonesia)
  </read_first>
  <files>backend/app/__init__.py, backend/app/core/config.py, backend/app/core/database.py, backend/app/models/__init__.py, backend/app/models/enums.py, backend/app/models/pengguna.py, backend/app/models/buku.py, backend/app/models/salinan_buku.py, backend/app/models/peminjaman.py, backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/alembic/versions/0001_initial_schema.py, backend/app/seed.py, backend/.env.example</files>
  <behavior>
    - Migration upgrade on clean Postgres creates ENUM types peran_pengguna, kondisi_buku, status_salinan, status_peminjaman and tables pengguna, buku, salinan_buku, peminjaman with all FKs
    - Migration downgrade drops tables then ENUM types in reverse dependency order
    - seed.run() inserts pustakawan@biblio.ac.id once; a second call detects the existing row and skips (idempotent)
    - Settings loads database_url, jwt_secret, jwt_algorithm (default HS256), jwt_expire_minutes (default 60) from .env
  </behavior>
  <action>
    Build the backend package per RESEARCH Recommended Project Structure. config.py: pydantic-settings Settings with fields database_url, jwt_secret, jwt_algorithm="HS256", jwt_expire_minutes=60 (env_file=".env", extra="ignore"). database.py: SQLAlchemy engine from settings.database_url, SessionLocal sessionmaker, get_db dependency yielding a session, and the declarative Base. enums.py: Python enum.Enum classes PeranPengguna(mahasiswa, pustakawan), KondisiBuku, StatusSalinan, StatusPeminjaman with the exact value members from PRD §4. Models use Mapped/mapped_column and reference PGEnum(..., create_type=False) per Pattern 1; pengguna has columns id (UUID PK, default uuid4), nama String(150), email String(100) unique+index, kata_sandi String(255), peran peran_pengguna_enum, is_diblokir Boolean default False server_default false, plus total_denda/timestamp columns only if PRD §4 specifies them. Use lowercase snake_case PG ENUM type names (peran_pengguna, kondisi_buku, status_salinan, status_peminjaman) per RESEARCH Pattern 2 note and Open Question 1 recommendation (Postgres folds unquoted identifiers to lowercase). Hand-write alembic/versions/0001_initial_schema.py per Pattern 2: create each ENUM via postgresql.ENUM(...).create(op.get_bind(), checkfirst=True), then create_table for all 4 tables referencing the enums with create_type=False; downgrade drops tables then enums. Do NOT use `alembic revision --autogenerate` (Pitfall 1). alembic/env.py uses python-dotenv to load .env and sets sqlalchemy.url from DATABASE_URL, target_metadata = Base.metadata. seed.py per Pattern 7: idempotent check-then-insert of pustakawan@biblio.ac.id (the hashing call is supplied by Plan 02's security.hash_password — import it; if Plan 02 not yet merged, this import is the documented seam). .env.example per RESEARCH snippet with placeholder JWT_SECRET=changeme_use_openssl_rand_hex_32 and the docker service-name DATABASE_URL (db host). Mitigates threats T-01-01 (SQL injection: ORM parameterized only, no raw SQL) and T-01-02 (plaintext secrets: .env.example placeholder only).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models.pengguna import Pengguna; from app.models.enums import PeranPengguna, KondisiBuku, StatusSalinan, StatusPeminjaman; import app.seed; print(PeranPengguna.mahasiswa.value)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create_type=False" backend/app/models/*.py` returns >= 4 (one per enum-typed column / model)
    - backend/alembic/versions/0001_initial_schema.py contains all four table names: pengguna, buku, salinan_buku, peminjaman
    - backend/alembic/versions/0001_initial_schema.py contains all four enum names: peran_pengguna, kondisi_buku, status_salinan, status_peminjaman
    - `grep -v '^#' backend/.env.example | grep -c "changeme"` returns >= 1 (placeholder secret only, never a real key)
    - The import smoke command prints `mahasiswa`
    - No raw string-formatted SQL: `grep -rn "execute(f\"" backend/app` returns nothing
  </acceptance_criteria>
  <done>Models import; migration file references all 4 tables + 4 enums with create_type=False pattern; seed is idempotent check-then-insert; .env.example has placeholder secret.</done>
</task>

<task type="auto">
  <name>Task 3: pytest Wave 0 scaffold — conftest fixtures + failing test_auth.py (RED)</name>
  <read_first>
    - .planning/phases/01-foundation-schema-auth/01-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map rows AUTH-01..04 with their exact test node IDs)
    - .planning/phases/01-foundation-schema-auth/01-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map, test node id names test_register / test_login_returns_jwt / test_password_is_hashed / test_protected_endpoint_401)
    - backend/app/core/database.py (the get_db dependency the TestClient fixture must override)
  </read_first>
  <files>backend/pytest.ini, backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_auth.py</files>
  <action>
    pytest.ini sets testpaths=tests and (optionally) `addopts = -ra`; no watch flags. conftest.py provides: a test DB session fixture (SQLite in-memory via StaticPool, OR a throwaway Postgres schema — SQLite is acceptable per VALIDATION Wave 0 note, but if enum types require Postgres semantics, create tables via Base.metadata.create_all on a test engine), a FastAPI TestClient fixture that overrides get_db with the test session, and a fixture creating the schema before tests. test_auth.py contains exactly these four tests matching VALIDATION node IDs: test_register (AUTH-01: POST /api/autentikasi/registrasi with email+password>=8 chars returns 201/200 + token; password<8 rejected; duplicate email rejected; stored peran==mahasiswa), test_login_returns_jwt (AUTH-02: POST /api/autentikasi/masuk returns a JWT whose decoded exp is ~60 min out), test_password_is_hashed (AUTH-03: stored pengguna.kata_sandi != plaintext and starts with `$2`), test_protected_endpoint_401 (AUTH-04: a protected endpoint returns 401 with no token and with an invalid/expired token). These tests MUST fail now (RED) because the endpoints do not exist yet — that is the intended Wave 0 state; do NOT stub the app to make them pass. After writing, run pytest and confirm they fail for the right reason (404/import error on the missing auth router), not a collection error.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_auth.py --co -q && echo "COLLECTED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_auth.py` returns 4
    - test_auth.py defines test_register, test_login_returns_jwt, test_password_is_hashed, test_protected_endpoint_401 (exact names matching VALIDATION map)
    - `python -m pytest tests/test_auth.py --co -q` collects 4 tests without a collection error (prints COLLECTED_OK)
    - Running `python -m pytest tests/test_auth.py` results in FAILURES (RED), not all-pass — the missing auth router causes the failures
    - pytest.ini contains no watch-mode flag (no --watch, no -f)
  </acceptance_criteria>
  <done>conftest provides TestClient + test DB session fixtures; test_auth.py has 4 named failing tests collectable by pytest; this is the RED baseline Plan 02 turns GREEN.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols/files created here (exclude from drift verification):
- Files: backend/requirements.txt, backend/requirements-dev.txt, backend/.env.example, backend/pytest.ini, backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/alembic/versions/0001_initial_schema.py, backend/app/__init__.py, backend/tests/__init__.py
- Config: `Settings` (pydantic-settings class) in app/core/config.py; module-level `settings`; fields database_url, jwt_secret, jwt_algorithm, jwt_expire_minutes
- DB: `Base` (DeclarativeBase), `engine`, `SessionLocal`, `get_db` in app/core/database.py
- Enums: `PeranPengguna`, `KondisiBuku`, `StatusSalinan`, `StatusPeminjaman` in app/models/enums.py; PG enum type names peran_pengguna, kondisi_buku, status_salinan, status_peminjaman
- Models: `Pengguna`, `Buku`, `SalinanBuku`, `Peminjaman` classes (app/models/*.py); tables pengguna, buku, salinan_buku, peminjaman
- Migration revision: `0001_initial_schema` (upgrade/downgrade)
- Seed: `run()` and constants PUSTAKAWAN_EMAIL, PUSTAKAWAN_PASSWORD in app/seed.py
- Tests: fixtures in conftest.py (test db session, TestClient); test functions test_register, test_login_returns_jwt, test_password_is_hashed, test_protected_endpoint_401
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> PyPI registry | [ASSUMED] packages installed from a public registry (slopcheck never ran) |
| application code -> PostgreSQL | All DB access crosses here; must be parameterized ORM only |
| filesystem (.env) -> application | Secrets (JWT key, DB creds) loaded from gitignored .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SQLAlchemy DB access | mitigate | ORM Mapped/mapped_column parameterized queries only; no raw f-string SQL (acceptance criterion greps for `execute(f"`) |
| T-01-02 | Information Disclosure | .env / .env.example secrets | mitigate | .env.example ships placeholder JWT_SECRET only (D-14); real secret via `openssl rand -hex 32` in gitignored .env; acceptance criterion greps for placeholder |
| T-01-SC | Tampering | pip installs (all [ASSUMED]) | mitigate | Blocking-human checkpoint (Task 1) verifies install + pwdlib/pyjwt legitimacy on pypi.org before any code depends on them |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_auth.py --co -q` collects 4 tests (RED expected on run)
- Migration file contains all 4 tables + 4 enums (grep)
- Models import without error; seed importable and idempotent by construction
- No raw SQL string formatting in app/
</verification>

<success_criteria>
- Backend package structure exists per RESEARCH layout
- AUTH-03 foundation in place (kata_sandi is a hash column; actual hashing wired in Plan 02)
- NFR-01 foundation: simple indexed pengguna queries (email index) keep auth well under 2s
- Wave 0 RED tests authored and collectable, ready for Plan 02 to turn GREEN
- All [ASSUMED] packages human-verified before use
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema-auth/01-01-SUMMARY.md` when done.
</output>
