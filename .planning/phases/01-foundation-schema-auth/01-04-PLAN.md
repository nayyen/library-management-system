---
phase: 01-foundation-schema-auth
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - backend/Dockerfile
  - backend/entrypoint.sh
  - frontend/Dockerfile
  - README.md
autonomous: false
requirements: [DEPLOY-01, NFR-01, NFR-02]
user_setup: []
must_haves:
  truths:
    - "docker compose up starts db, backend, and frontend, applies the migration, seeds the pustakawan, and serves the login page"
    - "The backend container waits for Postgres health, runs alembic upgrade head then the seed, then uvicorn --reload"
    - "Secrets come from a gitignored .env; .env.example documents every variable with safe defaults"
    - "A sample API call completes in under 2 seconds and the UI renders at 375px"
  artifacts:
    - path: "docker-compose.yml"
      provides: "3-service dev stack with Postgres healthcheck"
      contains: "service_healthy"
    - path: "backend/entrypoint.sh"
      provides: "migrate then seed then uvicorn startup sequence"
      contains: "alembic upgrade head"
    - path: ".env.example"
      provides: "documented env vars with local defaults"
      contains: "JWT_SECRET"
    - path: "README.md"
      provides: "run instructions + seeded pustakawan credentials"
      contains: "compose up"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/entrypoint.sh"
      via: "backend service build + entrypoint"
      pattern: "build:"
    - from: "docker-compose.yml frontend service"
      to: "backend service"
      via: "depends_on + Vite proxy network"
      pattern: "depends_on"
    - from: "backend entrypoint"
      to: "db service"
      via: "depends_on condition service_healthy"
      pattern: "service_healthy"
---

<objective>
Tie the three legs of the Walking Skeleton together into a one-command dev stack (DEPLOY-01): docker-compose with a Postgres healthcheck, a backend Dockerfile + entrypoint that runs alembic upgrade head then the seed then uvicorn --reload (D-15), a frontend dev Dockerfile (Vite --host), a gitignored .env with a committed .env.example (D-14), and a README documenting the run command + seeded pustakawan credentials. Then perform the manual smoke verification for the success criteria that cannot be automated in this WSL-without-Docker environment (DEPLOY-01, NFR-01, NFR-02).

Purpose: Make docker compose up produce a fully working, login-ready app — the final proof the full stack is wired correctly.
Output: docker-compose.yml, Dockerfiles, entrypoint, env files, .gitignore, README, and a passed manual smoke checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema-auth/01-CONTEXT.md
@.planning/phases/01-foundation-schema-auth/01-RESEARCH.md
@.planning/phases/01-foundation-schema-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose, Dockerfiles, entrypoint, env files, .gitignore, README</name>
  <read_first>
    - .planning/phases/01-foundation-schema-auth/01-RESEARCH.md (Pattern 6 docker-compose 3 services + healthcheck + backend/frontend Dockerfiles + entrypoint.sh; .env.example snippet; Environment Availability note — Docker not runnable inside this WSL session)
    - .planning/phases/01-foundation-schema-auth/01-CONTEXT.md (D-13 hot-reload dev volumes + uvicorn --reload + Vite; D-14 gitignored .env + committed .env.example; D-15 entrypoint migrate+seed before uvicorn; D-16 Vite proxy no CORS; Claude's Discretion: ports + seeded credentials documented in README)
    - backend/app/seed.py (seed entrypoint command python -m app.seed; PUSTAKAWAN_EMAIL / PUSTAKAWAN_PASSWORD to document in README)
    - backend/.env.example (Plan 01 backend env example — the root .env.example must stay consistent)
  </read_first>
  <files>docker-compose.yml, backend/Dockerfile, backend/entrypoint.sh, frontend/Dockerfile, .env.example, .gitignore, README.md</files>
  <action>
    docker-compose.yml per RESEARCH Pattern 6: service db (postgres:16-alpine; env POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB; pgdata named volume; healthcheck pg_isready interval 5s retries 5); backend (build ./backend; env_file .env; DATABASE_URL=postgresql+psycopg://USER:PASS@db:5432/DB; volume ./backend:/app; port 8000:8000; depends_on db condition service_healthy); frontend (build ./frontend; volumes ./frontend:/app plus anonymous /app/node_modules; port 5173:5173; depends_on backend). backend/Dockerfile: python:3.12-slim, install requirements.txt + requirements-dev.txt, chmod +x entrypoint.sh, ENTRYPOINT ./entrypoint.sh. backend/entrypoint.sh per D-15: set -e then alembic upgrade head then python -m app.seed then exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload. frontend/Dockerfile (dev): node:20-alpine, npm install, CMD npm run dev -- --host. Root .env.example per RESEARCH snippet: POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB, DATABASE_URL with db host, JWT_SECRET=changeme_use_openssl_rand_hex_32 (placeholder only), JWT_ALGORITHM=HS256, JWT_EXPIRE_MINUTES=60. .gitignore MUST include a line for .env (D-14 — never commit a real secret), plus node_modules, __pycache__, *.pyc. README.md: prerequisites (Docker Desktop with WSL2 integration per RESEARCH Environment Availability), the run command docker compose up, the seeded pustakawan credentials from seed.py (pustakawan@biblio.ac.id and its documented password), and instructions to copy .env.example to .env and generate JWT_SECRET via openssl rand -hex 32. Never commit a real secret. Mitigates threats T-04-01 (secret leakage) and T-04-02 (DB readiness race).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q service_healthy docker-compose.yml && grep -q "alembic upgrade head" backend/entrypoint.sh && grep -q "app.seed" backend/entrypoint.sh && grep -Eq "^\.env$" .gitignore && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml defines three services (db, backend, frontend) and contains service_healthy on the backend depends_on
    - backend/entrypoint.sh runs alembic upgrade head, then python -m app.seed, then exec uvicorn ... --reload, in that order
    - .gitignore contains a bare .env line (grep -Eq '^\.env$' .gitignore succeeds) — real secrets never committed
    - .env.example JWT_SECRET is the changeme placeholder (no real key); grep for changeme succeeds
    - README.md documents docker compose up and the seeded pustakawan email pustakawan@biblio.ac.id
    - docker-compose frontend proxy path: vite.config.js (Plan 03) already targets backend:8000 — no CORS service added (grep CORSMiddleware backend/app returns nothing)
  </acceptance_criteria>
  <done>One-command dev stack files exist; entrypoint migrates+seeds before uvicorn; .env gitignored with placeholder-only example; README documents run command + seeded credentials.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — docker compose up, sub-2s API, 375px responsive</name>
  <read_first>
    - .planning/phases/01-foundation-schema-auth/01-VALIDATION.md (Manual-Only Verifications table — DEPLOY-01, NFR-01, NFR-02 are manual because Docker is unavailable for automated runs in this WSL session)
    - .planning/phases/01-foundation-schema-auth/01-RESEARCH.md (Environment Availability: enable Docker Desktop WSL2 integration or run docker compose from a Windows shell)
    - README.md (the run instructions + seeded credentials authored in Task 1)
  </read_first>
  <files>(verification only — no files modified)</files>
  <what-built>
    Task 1 produced the full docker-compose dev stack. This task is the human smoke test of the three manual-only success criteria that cannot run automated in this environment.
  </what-built>
  <action>
    Human runs the stack and verifies DEPLOY-01, NFR-01, NFR-02 per the steps below. This is blocking because Docker is not runnable from this WSL session (RESEARCH Environment Availability) — the team member runs docker compose from a Windows shell or with WSL2 integration enabled.
  </action>
  <how-to-verify>
    1. From the repo root, copy .env.example to .env and set JWT_SECRET (openssl rand -hex 32). Run docker compose up. Confirm db becomes healthy, the backend logs show alembic upgrade head succeeding then the seed line (created or already-exists), and uvicorn starts. (DEPLOY-01)
    2. Open http://localhost:5173 in a browser — confirm the Biblio login page renders. (DEPLOY-01)
    3. Register a mahasiswa, then log in as pustakawan@biblio.ac.id — confirm both reach the welcome shell. (full-stack proof)
    4. Time a sample API call: curl -w "%{time_total}\n" -o /dev/null -s -X POST http://localhost:8000/api/autentikasi/masuk with valid JSON credentials — confirm the time is under 2 seconds. (NFR-01)
    5. In browser devtools, set the viewport to 375px and visit /login, /register, and the authenticated shell — confirm no overflow/clipping; inputs and buttons stay full-width and tappable. (NFR-02)
  </how-to-verify>
  <verify>
    <automated>MISSING — Docker unavailable in this WSL session; DEPLOY-01/NFR-01/NFR-02 are manual-only per 01-VALIDATION.md. Human verification via the steps above.</automated>
  </verify>
  <acceptance_criteria>
    - docker compose up brings all three containers up; backend log shows migration applied + seed run + uvicorn started
    - http://localhost:5173 serves the Biblio login page
    - The sample /api/autentikasi/masuk call returns in under 2 seconds (NFR-01)
    - /login, /register, and the shell render correctly at 375px with no overflow (NFR-02)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the stack runs, the login page loads, the API call is sub-2s, and the UI renders at 375px — or describe what failed.</resume-signal>
  <done>docker compose up produces a working login-ready app (DEPLOY-01); a sample API call is sub-2s (NFR-01); the UI renders at 375px (NFR-02).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols/files created here (exclude from drift verification):
- docker-compose.yml (services db, backend, frontend; pgdata volume)
- backend/Dockerfile, backend/entrypoint.sh
- frontend/Dockerfile
- .env.example (root), .gitignore, README.md
- Documented seeded credentials reference (pustakawan@biblio.ac.id) surfaced in README
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (.env.example) vs gitignored .env | Real secrets must never enter version control |
| backend container -> db container | Backend must not start migrations before Postgres is ready |
| host browser -> frontend/backend containers | Dev ports exposed on localhost for the demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | committed secrets | mitigate | .gitignore contains .env; .env.example ships changeme placeholder only; README instructs openssl rand -hex 32 (D-14) |
| T-04-02 | Denial of Service | DB readiness race | mitigate | depends_on db condition service_healthy + pg_isready healthcheck so alembic does not connect before Postgres accepts (RESEARCH Pattern 6 / A7) |
| T-04-03 | Information Disclosure | exposed dev ports | accept | localhost-only dev ports for a course-project demo (D-13 dev-optimized, not production-hardened) — accepted risk |
</threat_model>

<verification>
- cd repo root: grep confirms service_healthy, entrypoint order (alembic then seed), .env gitignored, README run command
- Manual smoke (Task 2) verifies DEPLOY-01, NFR-01, NFR-02 (manual-only per VALIDATION)
</verification>

<success_criteria>
- DEPLOY-01: docker compose up runs db + backend + frontend; login page loads; migration + seed run automatically (D-15)
- NFR-01: sample API call under 2s
- NFR-02: login/register/shell render at 375px
- D-13/D-14/D-15/D-16 honored; secrets never committed
</success_criteria>

<output>
Create `.planning/phases/01-foundation-schema-auth/01-04-SUMMARY.md` when done.
</output>
